version: '3.8'

services:
    php:
        build: 
            ./php
        links:
            - postgresql
        networks:
            - dockernet
    postgresql:
        image: postgres:16-alpine3.17
        environment:
            - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
            - POSTGRES_USER=${POSTGRES_USER}
        volumes:
            - postgresdb:/usr/src/data/db
        networks:
            - dockernet
    nginx:
        build:
            context: .
            dockerfile: nginx/Dockerfile
        ports:
            - ${port}:${port}
        volumes:
            - ./php/project:/usr/src/project/
            - ./logs/nginx/:/usr/src/log/nginx/
        networks:
            - dockernet
    adminer:
        image: adminer:latest
        networks:
            - dockernet
volumes:
  postgresdb:
networks:
  dockernet:
    driver: bridge